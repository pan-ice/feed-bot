# 🍞 投喂插件 (feed-bot)

MaiBot 投喂插件 — 签到积分 + 商店道具 + 投喂Bot + 定时求投喂。

支持全局/群内两层商店、群管理员授权、按群隔离数据，以及自定义商品饱食度和签到积分。

## 功能特性

- **签到系统**：每日签到获取积分，连续签到有额外奖励；基础积分可由 Bot 管理员私聊调整并持久化
- **积分系统**：记录每个用户的积分余额，支持本群积分排行
- **商店系统**：全局商店（Bot管理员管理）+ 群内商店（群管理员管理）；`/商店` 展示时每个商品之间隔一行，方便查看
- **批量购买/投喂**：`/购买` 和 `/投喂` 支持在道具名后追加数量，单次最多 99 个
- **投喂系统**：用道具投喂Bot，触发 LLM 生成的趣味回复；饱食度满时无法继续投喂；道具饱食度可由管理员自定义（支持正负值）
- **Bot属性**：饱食度随投喂增加、随时间衰减，低于阈值时 Bot 会定时在群中求投喂
- **规则查询**：`/投喂规则` 一键查看非管理员可使用的全部指令

## 命令列表

### 用户命令（所有成员可用，部分命令仅限群聊）

| 命令 | 说明 |
|------|------|
| `/签到` | 每日签到获取积分 |
| `/积分` | 查看积分余额 |
| `/积分排行` | 查看本群积分排行（仅群聊） |
| `/商店` | 查看可购买道具（每个商品之间隔一行） |
| `/购买 <道具名> [x数量]` | 购买道具放入背包，单次最多99个 |
| `/背包` | 查看背包道具 |
| `/投喂 <道具名> [x数量]` | 投喂Bot，单次最多99个（仅群聊） |
| `/投喂记录` | 查看投喂历史 |
| `/饱食度` | 查看当前饱食度（仅群聊） |
| `/投喂排行` | 查看本群投喂排行（仅群聊） |
| `/投喂规则` | 查看非管理员可使用的全部指令 |

数量写法示例：

- `/购买 面包 x5`、`/购买 面包 *5`、`/购买 面包 ×5`（前缀写法）
- `/购买 面包 5`、`/投喂 面包 3`（直接空格加数字）

数量前缀支持 `x`、`X`、`*`、`×`，单次最多 99 个。
购买时积分不足则整批不执行；投喂时库存不足则整批不执行；饱食度容量不足时只消耗实际能吃下的数量。

### Bot管理员命令（仅私聊）

| 命令 | 说明 |
|------|------|
| `/投喂管理 全局上架 <名称> <价格> [emoji] [描述] [饱食度N]` | 上架全局道具 |
| `/投喂管理 全局下架 <道具名>` | 下架全局道具 |
| `/投喂管理 签到积分 <数值>` | 设置全局签到基础积分（0-1,000,000，持久化保存） |

### 管理员命令（群聊中群管理员可用，私聊中Bot管理员可用）

| 命令 | 说明 |
|------|------|
| `/投喂管理 修改饱食度 <全局|群> <道具名> <饱食度>` | 修改道具饱食度增加值（需指定全局或群范围） |
| `/投喂管理 积分 <QQ号> <数量> [群号]` | 增减用户积分（私聊时需指定群号） |
| `/投喂管理 属性 <属性名> <值> [群号]` | 设置Bot属性（属性名支持 satiety / 饱食度，私聊时需指定群号） |
| `/投喂管理 重置签到 <QQ号> [群号]` | 重置签到记录（私聊时需指定群号） |
| `/投喂管理 授权 <群号> <QQ号>` | 授权群管理员 |
| `/投喂管理 取消授权 <群号> <QQ号>` | 取消群管理员 |
| `/投喂管理 群列表` | 查看已授权的群及管理员 |

### 群管理员命令

| 命令 | 说明 |
|------|------|
| `/投喂管理 群上架 <名称> <价格> [emoji] [描述] [饱食度N]` | 上架群内道具 |
| `/投喂管理 群下架 <道具名>` | 下架群内道具 |

上架示例：

- `/投喂管理 群上架 面包 10 🍞 好吃的 饱食度10`
- `/投喂管理 群上架 面包 10 🍞 好吃的`
- `/投喂管理 群上架 面包 10`

> 如需设置道具饱食度，请在命令末尾添加“饱食度”三个字和数值，例如 `饱食度10`。

## 自定义商品饱食度

每个道具的饱食度（`satiety_bonus`）表示投喂后饱食度的增减量，支持整数和小数、正数和负数。

**方式一：上架时指定**

在群上架或全局上架命令末尾追加 `饱食度<数值>`：

- `/投喂管理 群上架 面包 10 🍞 好吃的 饱食度10` → 投喂后饱食度 +10
- `/投喂管理 群上架 毒蘑菇 5 🍄 慎用 饱食度-5` → 投喂后饱食度 -5

不写饱食度时默认 `+5`。

**方式二：修改已有道具**

`/投喂管理 修改饱食度 <全局|群> <道具名> <数值>`

- `/投喂管理 修改饱食度 群 面包 8` → 修改本群道具“面包”的饱食度为 +8
- `/投喂管理 修改饱食度 全局 面包 12.5` → 修改全局道具“面包”的饱食度为 +12.5

修改群道具需要在群聊中执行；私聊中修改全局道具需 Bot 管理员权限。

## 自定义签到积分

签到积分 = `基础积分 + min((连续天数-1) × 递增奖励, 奖励上限)`

**方式一：Bot管理员私聊指令（推荐，实时生效且持久化）**

`/投喂管理 签到积分 <数值>`

例如 Bot 管理员私聊执行 `/投喂管理 签到积分 20` 后，每次签到的全局基础积分变为 20。允许范围为 0-1,000,000，设置值保存在数据库中，重启插件不会丢失。

**方式二：config.toml 配置**

编辑插件目录下的 `config.toml` 的 `[sign]` 段，修改后热重载生效：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `base_points` | int | `10` | 每次签到获得的基础积分 |
| `consecutive_bonus` | int | `5` | 连续签到每天额外递增的奖励积分 |
| `max_consecutive_bonus` | int | `50` | 连续签到奖励积分上限 |

> 优先级：Bot 管理员指令设置的持久化值优先于 config.toml 中的 `base_points`。

示例（`base_points=10, consecutive_bonus=5, max_consecutive_bonus=50`）：

| 连续签到天数 | 签到获得积分 |
|-------------|-------------|
| 第1天 | 10 + 0 = 10 |
| 第2天 | 10 + 5 = 15 |
| 第3天 | 10 + 10 = 20 |
| 第11天 | 10 + 50 = 60（达到上限） |

## 配置说明

编辑插件目录下的 `config.toml` 进行配置，修改后支持热重载。

### `[plugin]` 插件基础

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `enabled` | bool | `true` | 是否启用投喂插件 |
| `config_version` | string | `"1.1.1"` | 配置版本号 |

### `[admin]` 管理员

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `admin_users` | list[str] | `[]` | Bot管理员QQ号列表，拥有所有管理员权限 |

```toml
[admin]
admin_users = ["123456789", "987654321"]
```

### `[sign]` 签到

见上方「自定义签到积分」章节。

### `[bot_attr]` Bot属性

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 初始饱食度（`initial_satiety`） | float | `80.0` | 初始饱食度（0-100） |
| 饱食度衰减量（`satiety_decay_rate`） | float | `0.5` | 饱食度每小时衰减量 |
| 求投喂阈值（`seek_feed_threshold`） | float | `30.0` | 饱食度低于此值触发求投喂 |
| 求投喂冷却时间（`seek_feed_cooldown`） | float | `7200.0` | 求投喂消息冷却时间（秒，默认2小时） |
| 安静时段（`quiet_hours`） | string | `"23:00-08:00"` | 求投喂安静时段，格式 `HH:MM-HH:MM`（24小时制），该时段内不发送求投喂消息；留空则不限制 |
| 求投喂消息（`seek_feed_messages`） | list[str] | (5条默认消息) | 求投喂消息列表，随机选择一条发送（LLM开启时优先用LLM生成） |

属性机制：

- **饱食度**：投喂道具时增加，增加量由道具的 `satiety_bonus` 决定，每个道具不同
- 饱食度上限为 100，达到满饱食度时无法继续投喂
- 每小时自动衰减 `satiety_decay_rate`
- 饱食度低于 `seek_feed_threshold` 时，Bot 会定时在群中发送求投喂消息
- 求投喂消息从 `seek_feed_messages` 列表中随机选择一条发送；LLM 开启时优先用 LLM 生成更自然的回复

所有数据按群隔离：饱食度、积分、签到记录、背包道具、投喂记录，同一个QQ在不同群的数据互不影响。管理员命令在私聊中操作时需额外指定群号。

### `[filter]` 群配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `group_admins` | list[GroupAdminEntry] | `[]` | 授权群配置。**只有在列表中的群才会响应命令**，未配置的群静默忽略。运行时可通过 `/投喂管理 授权` 追加管理员，通过 `/投喂管理 群列表` 查看。 |

每个 `GroupAdminEntry` 包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `group_id` | str | 群号 |
| `admin_users` | str | 管理员QQ号，多个用逗号、空格或 \| 分隔 |

```toml
[[filter.group_admins]]
group_id = "123456789"
admin_users = "111, 222"

[[filter.group_admins]]
group_id = "987654321"
admin_users = "333 | 444"
```

### `[llm]` LLM回复

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `enabled` | bool | `true` | 是否使用LLM生成投喂回复（关闭则使用 `fallback_reply`） |
| `model` | string | `"replyer"` | LLM模型名，留空使用MaiBot的replyer模型 |
| `temperature` | float | `0.8` | 生成温度，越高越随机（0.0-2.0） |
| `max_tokens` | int | `300` | 单次回复最大token数（含reasoning token） |
| `fallback_reply` | string | `"谢谢你投喂我！好开心~"` | LLM不可用时的兜底回复 |

## 数据存储

- 所有数据保存在插件目录的 `data/feed_bot.db`（SQLite，自动创建）
- 用户积分、签到、背包、投喂记录、群管理员授权、签到积分设置均存储在数据库中
- 按群隔离：群号前缀区分不同群的用户数据

## 安装

1. 将插件目录放置到 MaiBot 的 `plugins/` 目录下
2. 重启 MaiBot，或通过 WebUI 插件管理刷新/重载插件
3. 首次启动自动生成插件目录下的 `config.toml`
4. 在 `config.toml` 中配置 `[admin].admin_users`（Bot管理员）和 `[[filter.group_admins]]`（授权群）后即可使用

## 测试

在插件目录执行：

```bash
uv run --project ../.. pytest -q --rootdir=../.. --import-mode=importlib tests
```

## 权限体系

| 权限级别 | 识别方式 | 可执行操作 |
|---------|---------|-----------|
| Bot管理员 | `config.toml` 中 `admin_users` 列表 | 所有管理员命令 + 全局商店管理 + 全局签到积分设置 + 群内商店管理 + 授权群管理员 |
| 群管理员 | 通过配置 `group_admins` 或 `/投喂管理 授权` 设置 | 本群的群内商店管理（上架/下架群内道具）+ 授权/取消授权本群管理员 |
| 普通用户 | 默认 | 签到、查看积分、购买、投喂、查看规则等用户命令 |

## 项目结构

| 文件 | 说明 |
|------|------|
| `plugin.py` | 插件入口与生命周期管理 |
| `commands_user.py` | 用户命令（签到、积分、商店、购买、背包、投喂、规则等） |
| `commands_admin.py` | 管理员命令（商店管理、积分调整、签到设置、授权管理等） |
| `db.py` | SQLite 异步数据库层 |
| `loops.py` | 定时任务（饱食度衰减、求投喂）与 LLM 回复生成 |
| `config.py` | 配置模型定义 |
| `utils.py` | 工具函数与路径常量 |

## 更新记录

- **2026-08-05**：
  - `/商店` 展示时每个商品之间隔一行，便于查看
  - `/购买`、`/投喂` 支持直接空格加数字的数量写法，单次数量上限调整为 99
  - 新增 `/投喂规则`，可查看非管理员可使用的全部指令
  - 新增 Bot 管理员私聊命令 `/投喂管理 签到积分 <数值>`，可实时设置全局签到基础积分并持久化

## 许可证

MIT License
